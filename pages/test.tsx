import type { NextPage } from "next";
import { CustomCard } from "../shared/presentation-components";

const Home: NextPage = () => {
  return <CustomCard>Element</CustomCard>;
};

export default Home;
